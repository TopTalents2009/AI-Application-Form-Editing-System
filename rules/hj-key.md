# HJ · 关键申报信息

只改正文封面栏（国籍、姓名、实验室、专业领域、学科、前沿领域等）。

1. 「所属二级学科及代码」是下拉框，replace 必须与表内选项全文一致，禁止自拟。
   - 计算机方向一级：`520计算机科学技术`（不要写成计算机科学、计算机架构）。
   - 二级与「计算机架构 / 计算机体系结构」对应：`52030计算机系统机构`（表内用「机构」）。
   - 二级与「计算机应用」对应：`52060计算机应用`。
2. 改学科时必须同时改一级（I 列上一行）和二级（下一行），各一条 edit；两条 find 分别是当前格全文，例如 `470动力与电气工程`、`47040电气工程`。
3. 不要改提示语、公式单元格或 True/False 勾选残留。
4. 找不到与意见一致的下拉项时写入 leftovers，不要硬写非法值。
